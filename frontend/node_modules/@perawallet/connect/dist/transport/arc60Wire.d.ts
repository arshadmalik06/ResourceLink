/// <reference types="node" />
/// <reference types="node" />
import { PeraWalletArc60SignData, PeraWalletArc60SignDataResponse, SignMetadata } from "../util/model/peraWalletModels";
type Arc60WirePayload = Pick<PeraWalletArc60SignData, "data" | "signer" | "domain" | "authenticatorData" | "requestId" | "hdPath">;
/**
 * The bytes the wallet actually signs. Mirrors `buildArc60WireParams` — keep
 * the two in step, or local verification will hash different bytes than the
 * wallet did.
 */
declare function decodeArc60SignedData(data: string, encoding: SignMetadata["encoding"]): Buffer;
declare function buildArc60WireParams(payload: Arc60WirePayload, metadata: SignMetadata): Record<string, unknown>;
declare function buildArc60SignDataResponse(payload: Arc60WirePayload, signature: Uint8Array): PeraWalletArc60SignDataResponse;
export { buildArc60WireParams, buildArc60SignDataResponse, decodeArc60SignedData };
