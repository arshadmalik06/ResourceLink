import { WalletTransport, ConnectOptions } from "../WalletTransport";
import { Arc0027Client } from "./arc0027Client";
import { DiscoverResult } from "./arc0027Types";
import { PeraWalletArbitraryData, PeraWalletArc60SignData, PeraWalletArc60SignDataResponse, SignMetadata, PeraWalletTransaction } from "../../util/model/peraWalletModels";
import { AlgorandChainIDs } from "../../util/peraWalletTypes";
export declare class ExtensionTransport implements WalletTransport {
    readonly platform: "extension";
    private client;
    constructor(client?: Arc0027Client);
    static discover(client?: Arc0027Client): Promise<DiscoverResult | null>;
    connect(_opts?: ConnectOptions): Promise<string[]>;
    reconnect(): Promise<string[]>;
    disconnect(): Promise<void>;
    signTransaction(txns: PeraWalletTransaction[], _signerAddress?: string): Promise<Uint8Array[]>;
    signData(_data: PeraWalletArbitraryData[], _signer: string, _chainId: AlgorandChainIDs): Promise<Uint8Array[]>;
    signArc60Data(payload: PeraWalletArc60SignData, metadata: SignMetadata): Promise<PeraWalletArc60SignDataResponse>;
}
