import { Arc0027Method, DiscoverResult } from "./arc0027Types";
export declare class Arc0027RequestError extends Error {
    code: number;
    constructor(code: number, message: string);
}
export declare class Arc0027Client {
    private target;
    constructor(target?: Window);
    discover(timeoutMs?: number): Promise<DiscoverResult | null>;
    request(method: Arc0027Method, params: Record<string, unknown>, timeoutMs?: number): Promise<Record<string, unknown>>;
}
