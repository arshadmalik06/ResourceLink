export declare const WAIT_FOR_TAB_TRY_INTERVAL = 700;
export declare const WAIT_FOR_TAB_MAX_TRY_COUNT = 50;
declare function getMetaInfo(): {
    title: string;
    description: string;
    url: string;
    favicon: string;
};
declare function getFavicons(): string[];
declare function waitForElementCreatedAtShadowDOM(element: Element, className: string): Promise<Element>;
/**
 * Navigates the current tab to the given URL. For custom-scheme deep links this
 * launches the app without unloading the page, unlike window.open(url, "_blank"),
 * which spawns an extra browsing context (iOS Safari leaves the dApp on a blank
 * or reloaded tab after returning from the wallet).
 */
declare function openDeepLinkInCurrentTab(url: string): void;
declare function waitForTabOpening(url: string): Promise<Window | null>;
export { getMetaInfo, getFavicons, waitForElementCreatedAtShadowDOM, openDeepLinkInCurrentTab, waitForTabOpening };
