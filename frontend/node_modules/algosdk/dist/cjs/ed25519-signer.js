"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.addressWithSignersFromRawEd25519Signer = void 0;
const address_js_1 = require("./encoding/address.js");
const encoding_js_1 = require("./encoding/encoding.js");
const logicsig_js_1 = require("./logicsig.js");
const multisig_js_1 = require("./multisig.js");
const signedTransaction_js_1 = require("./signedTransaction.js");
const signing_js_1 = require("./signing.js");
const utils_js_1 = require("./utils/utils.js");
function addressWithSignersFromRawEd25519Signer(ed25519SigningKey, sendingAddress = new address_js_1.Address(ed25519SigningKey.ed25519PublicKey)) {
    const authAddress = new address_js_1.Address(ed25519SigningKey.ed25519PublicKey);
    const { ed25519Signer: rawSigner } = ed25519SigningKey;
    const txnSigner = async (txnGroup, indexesToSign) => {
        const stxns = [];
        for (const index of indexesToSign) {
            const txn = txnGroup[index];
            // eslint-disable-next-line no-await-in-loop
            const sig = await rawSigner(txn.bytesToSign());
            const stxn = new signedTransaction_js_1.SignedTransaction({
                txn,
                sig,
                sgnr: txn.sender.equals(authAddress) ? undefined : authAddress,
            });
            stxns.push(stxn);
        }
        return stxns.map((stxn) => (0, encoding_js_1.encodeMsgpack)(stxn));
    };
    const delegatedLsigSigner = async (lsig, msig) => {
        if (msig) {
            const pks = (0, multisig_js_1.pksFromAddresses)(msig.addrs);
            const multisigAddr = (0, multisig_js_1.addressFromMultisigPreImg)({
                version: msig.version,
                threshold: msig.threshold,
                pks,
            });
            const toBeSigned = (0, utils_js_1.concatArrays)(logicsig_js_1.MSIG_PROGRAM_TAG, multisigAddr.publicKey, lsig.logic);
            const subsigs = pks.map((pk) => ({ pk }));
            const ourSubsig = subsigs.find((subsig) => (0, utils_js_1.arrayEqual)(subsig.pk, authAddress.publicKey));
            if (ourSubsig === undefined) {
                throw Error(`DelegatedLsigSigner could not find ${authAddress} as a signer in the multisig`);
            }
            const sig = await rawSigner(toBeSigned);
            ourSubsig.s = sig;
            return {
                address: authAddress,
                lmsig: {
                    v: msig.version,
                    thr: msig.threshold,
                    subsig: subsigs,
                },
            };
        }
        const toBeSigned = (0, utils_js_1.concatArrays)(logicsig_js_1.PROGRAM_TAG, lsig.logic);
        const sig = await rawSigner(toBeSigned);
        return { address: authAddress, sig };
    };
    const mxBytesSigner = async (bytes) => {
        const bytesToSign = (0, utils_js_1.concatArrays)(signing_js_1.SIGN_BYTES_PREFIX, bytes);
        return rawSigner(bytesToSign);
    };
    const programDataSigner = async (data, lsig) => {
        const parts = (0, utils_js_1.concatArrays)(lsig.address().publicKey, data);
        const toBeSigned = (0, utils_js_1.concatArrays)(logicsig_js_1.SIGN_PROGRAM_DATA_PREFIX, parts);
        return rawSigner(toBeSigned);
    };
    return {
        address: sendingAddress,
        programDataSigner,
        txnSigner,
        delegatedLsigSigner,
        mxBytesSigner,
    };
}
exports.addressWithSignersFromRawEd25519Signer = addressWithSignersFromRawEd25519Signer;
//# sourceMappingURL=ed25519-signer.js.map