import { NamedMapSchema, FixedLengthByteArraySchema, ByteArraySchema, Uint64Schema, ArraySchema, OptionalSchema, allOmitEmpty, } from '../../encoding/schema/index.js';
import { ensureSafeUnsignedInteger } from '../../utils/utils.js';
import { PQ_SALT_MAX, PQ_SCHEME_SIZE } from '../../encoding/address.js';
export const ENCODED_SUBSIG_SCHEMA = new NamedMapSchema(allOmitEmpty([
    {
        key: 'pk',
        valueSchema: new FixedLengthByteArraySchema(32),
    },
    {
        key: 's',
        valueSchema: new OptionalSchema(new FixedLengthByteArraySchema(64)),
    },
]));
export function encodedSubsigFromEncodingData(data) {
    if (!(data instanceof Map)) {
        throw new Error(`Invalid decoded EncodedSubsig: ${data}`);
    }
    const subsig = {
        pk: data.get('pk'),
    };
    if (data.get('s')) {
        subsig.s = data.get('s');
    }
    return subsig;
}
export function encodedSubsigToEncodingData(subsig) {
    const data = new Map([['pk', subsig.pk]]);
    if (subsig.s) {
        data.set('s', subsig.s);
    }
    return data;
}
export const ENCODED_MULTISIG_SCHEMA = new NamedMapSchema(allOmitEmpty([
    {
        key: 'v',
        valueSchema: new Uint64Schema(),
    },
    {
        key: 'thr',
        valueSchema: new Uint64Schema(),
    },
    {
        key: 'subsig',
        valueSchema: new ArraySchema(ENCODED_SUBSIG_SCHEMA),
    },
]));
export function encodedMultiSigFromEncodingData(data) {
    if (!(data instanceof Map)) {
        throw new Error(`Invalid decoded EncodedMultiSig: ${data}`);
    }
    return {
        v: ensureSafeUnsignedInteger(data.get('v')),
        thr: ensureSafeUnsignedInteger(data.get('thr')),
        subsig: data.get('subsig').map(encodedSubsigFromEncodingData),
    };
}
export function encodedMultiSigToEncodingData(msig) {
    return new Map([
        ['v', msig.v],
        ['thr', msig.thr],
        ['subsig', msig.subsig.map(encodedSubsigToEncodingData)],
    ]);
}
export const ENCODED_PQSIG_SCHEMA = new NamedMapSchema(allOmitEmpty([
    {
        key: 'sch',
        valueSchema: new ByteArraySchema(),
    },
    {
        key: 'slt',
        valueSchema: new Uint64Schema(),
    },
    {
        key: 'pk',
        valueSchema: new ByteArraySchema(),
    },
    {
        key: 'sig',
        valueSchema: new ByteArraySchema(),
    },
]));
export function encodedPQSigFromEncodingData(data) {
    if (!(data instanceof Map)) {
        throw new Error(`Invalid decoded EncodedPQSig: ${data}`);
    }
    const sch = data.get('sch');
    if (sch.length !== PQ_SCHEME_SIZE) {
        throw new Error(`Invalid decoded EncodedPQSig: expected a ${PQ_SCHEME_SIZE}-byte scheme, got ${sch.length} bytes`);
    }
    // The salt occupies a single byte of the address preimage, so anything wider
    // could never have produced a valid address.
    const slt = ensureSafeUnsignedInteger(data.get('slt'));
    if (slt > PQ_SALT_MAX) {
        throw new Error(`Invalid decoded EncodedPQSig: salt ${slt} exceeds the maximum of ${PQ_SALT_MAX}`);
    }
    return {
        sch,
        slt,
        pk: data.get('pk'),
        sig: data.get('sig'),
    };
}
export function encodedPQSigToEncodingData(pqsig) {
    return new Map([
        ['sch', pqsig.sch],
        ['slt', pqsig.slt],
        ['pk', pqsig.pk],
        ['sig', pqsig.sig],
    ]);
}
//# sourceMappingURL=encoded.js.map