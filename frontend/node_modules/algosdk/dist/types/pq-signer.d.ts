import { Address } from './encoding/address.js';
import { AddressWithDelegatedLsigSigner, AddressWithEmptyTransactionSigner, AddressWithTransactionSigner } from './signer.js';
export interface PQSigningKey {
    pqScheme: Uint8Array;
    pqPublicKey: Uint8Array;
    pqSigner: (bytesToSign: Uint8Array) => Promise<Uint8Array>;
}
export declare function addressWithSignersFromRawPQSigner(signingKey: PQSigningKey, sendingAddress?: Address): AddressWithTransactionSigner & AddressWithDelegatedLsigSigner & AddressWithEmptyTransactionSigner;
